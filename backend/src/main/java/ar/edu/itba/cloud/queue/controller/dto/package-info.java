/**
 * HTTP request payloads.
 *
 * <p>Only inputs live here. Responses are the immutable read models from
 * {@code ar.edu.itba.cloud.queue.service.model}, which are already the service layer's public shape -
 * re-declaring them here would add a mapping step without adding a boundary.
 */
package ar.edu.itba.cloud.queue.controller.dto;
