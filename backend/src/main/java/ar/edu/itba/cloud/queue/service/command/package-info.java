/**
 * Inputs accepted by the service layer.
 *
 * <p>Controllers translate HTTP request bodies into these, so services stay free of web concerns and
 * can be driven directly from tests.
 */
package ar.edu.itba.cloud.queue.service.command;
