# Java / Maven
target/
*.class
*.jar
!.mvn/wrapper/maven-wrapper.jar
.mvn/timing.properties

# IDE
.idea/
*.iml
*.iws
.vscode/
.settings/
.classpath
.project

# OS
.DS_Store

# Node / Next (frontend, later)
node_modules/
.next/
out/
.env*.local

# Local config and secrets. `.env.example` is the committed template; `.env` never is.
.env
.env.local
!.env.example
backend/src/main/resources/application-local.yml

# Local tooling
.claude/
